# 打印系统样式指南 - Notion/Youmind 风格

本文档记录了项目从传统样式升级到 Notion/Youmind 现代风格的设计规范。

## 🎨 设计理念

### 核心特点
- **简洁优雅** - 减少视觉噪音，注重内容本身
- **柔和舒适** - 使用温暖、柔和的配色方案
- **呼吸感** - 增加留白空间，提升阅读体验
- **流畅交互** - 细腻的过渡动画，提升用户体验

## 📐 设计规范

### 1. 色彩系统

#### 主色调
```css
--primary-color: #2383E2;      /* 主色 - 蓝色 */
--primary-hover: #0B6BCB;      /* 主色悬停态 */
--success-color: #46A758;      /* 成功色 - 绿色 */
--warning-color: #F59E0B;      /* 警告色 - 橙色 */
--error-color: #E5484D;        /* 错误色 - 红色 */
```

#### 文本颜色
```css
--text-primary: #37352F;       /* 主文本 - 深棕色 */
--text-secondary: #787774;     /* 次要文本 - 灰褐色 */
--text-tertiary: #9B9A97;      /* 三级文本 - 浅灰褐色 */
--text-disabled: #D1D1CF;      /* 禁用文本 */
```

#### 背景颜色
```css
--bg-base: #FFFFFF;            /* 基础背景 - 白色 */
--bg-light: #F7F6F3;           /* 浅色背景 - 米白色 */
--bg-hover: #F1F0ED;           /* 悬停背景 */
```

#### 边框颜色
```css
--border-color: #E8E7E4;       /* 边框颜色 */
--divider-color: #EDECE9;      /* 分割线颜色 */
```

### 2. 间距系统

采用 4px 基准的间距系统，提供更大的呼吸感：

```css
--spacing-xs: 4px;
--spacing-sm: 8px;
--spacing-md: 12px;
--spacing-lg: 20px;
--spacing-xl: 32px;
--spacing-xxl: 48px;
```

### 3. 字体系统

#### 字号规范
```css
--font-size-h1: 40px;          /* 一级标题 */
--font-size-h2: 30px;          /* 二级标题 */
--font-size-h3: 24px;          /* 三级标题 */
--font-size-h4: 18px;          /* 四级标题 */
--font-size-lg: 16px;          /* 大号正文 */
--font-size-base: 14px;        /* 正文 */
--font-size-sm: 12px;          /* 小号文本 */
```

#### 行高和字重
```css
--line-height-h: 1.2;          /* 标题行高 */
--line-height-base: 1.6;       /* 正文行高 */
--font-weight-bold: 600;       /* 粗体 */
--font-weight-medium: 500;     /* 中粗 */
--font-weight-normal: 400;     /* 常规 */
```

#### 字体栈
使用系统原生字体，确保跨平台的一致性和性能：
```css
font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 
  'Oxygen', 'Ubuntu', 'Cantarell', 'Fira Sans', 'Droid Sans', 
  'Helvetica Neue', 'PingFang SC', 'Hiragino Sans GB', 
  'Microsoft YaHei', '微软雅黑', sans-serif;
```

### 4. 阴影系统

采用 Notion 风格的轻量阴影，营造轻盈的视觉效果：

```css
--shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.04);
--shadow-md: 0 2px 8px rgba(0, 0, 0, 0.08);
--shadow-lg: 0 4px 16px rgba(0, 0, 0, 0.12);
--shadow-xl: 0 8px 32px rgba(0, 0, 0, 0.16);
--shadow-hover: 0 4px 12px rgba(0, 0, 0, 0.1);
```

### 5. 圆角系统

更圆润的圆角设计，提升现代感：

```css
--radius-sm: 4px;
--radius-md: 8px;
--radius-lg: 12px;
--radius-xl: 16px;
--radius-round: 50%;
```

### 6. 动画系统

流畅的过渡效果：

```css
--transition-fast: 0.15s;
--transition-base: 0.2s;
--transition-slow: 0.3s;
--ease-out: cubic-bezier(0, 0, 0.2, 1);
--ease-in-out: cubic-bezier(0.4, 0, 0.2, 1);
```

## 🎯 组件样式规范

### 卡片 (Card)
- 边框：1px solid var(--border-color)
- 圆角：var(--radius-lg)
- 阴影：var(--shadow-sm)
- 悬停：var(--shadow-hover)

### 按钮 (Button)
- 圆角：var(--radius-md)
- 字重：var(--font-weight-medium)
- 悬停：向上移动 1px，增加阴影
- 主按钮背景：var(--primary-color)

### 输入框 (Input)
- 圆角：var(--radius-md)
- 边框：1px solid var(--border-color)
- 悬停：边框颜色变深
- 焦点：蓝色边框 + 3px 阴影光晕

### 表格 (Table)
- 表头背景：var(--bg-light)
- 表头字重：var(--font-weight-medium)
- 表头颜色：var(--text-secondary)
- 行悬停：var(--bg-hover)

### 侧边栏 (Sidebar)
- 主题：light（亮色）
- 背景：var(--bg-base)
- 边框：1px solid var(--border-color)
- Logo：透明背景，带下边框

### 导航菜单
- 选中项：浅色背景，无强烈对比
- 悬停：var(--bg-hover)
- 字重：medium（选中时）
- 图标：与文本颜色一致

## 📱 响应式设计

保持原有的响应式断点：
- 移动设备：< 768px
- 平板：768px - 1023px
- 小桌面：1024px - 1279px
- 桌面：1280px - 1919px
- 超宽屏：≥ 1920px

## 🎨 设计细节

### 1. 滚动条
- 透明背景
- 圆角滑块
- 悬停时颜色加深

### 2. 代码块
- 背景：rgba(135, 131, 120, 0.15)
- 文本颜色：#EB5757
- 圆角：4px
- 内边距：2px 6px

### 3. 字间距
- 标题：-0.02em（紧凑）
- 正文：-0.003em（微调）

### 4. 交互反馈
- 按钮悬停：轻微上移（1px）
- 按钮点击：回到原位
- 卡片悬停：增强阴影
- 菜单项：背景色变化

## 🔄 迁移指南

### 已更新的文件
1. `src/index.css` - 全局样式和变量
2. `src/App.css` - 应用根组件样式
3. `src/pages/auth/Login.module.css` - 登录页面 CSS 模块
4. `src/pages/auth/Login.jsx` - 登录页面组件（使用 CSS 模块）
5. `src/components/layout/MainLayout.module.css` - 主布局 CSS 模块
6. `src/components/layout/MainLayout.jsx` - 主布局组件（使用 CSS 模块）

### 主要变更
- ✅ 色彩方案从蓝色系调整为 Notion 风格的柔和色调
- ✅ 阴影从较重调整为轻量级
- ✅ 圆角从 4px/8px 增加到 8px/12px
- ✅ 侧边栏从深色主题改为浅色主题
- ✅ 增加全局 Ant Design 组件样式覆盖
- ✅ 优化交互动画和过渡效果
- ✅ 调整间距和留白，提升呼吸感
- ✅ **从 Tailwind CSS 迁移到原生 CSS 模块化架构**

### CSS 架构升级 (2025-12-01)

#### 背景
项目原本混合使用全局 CSS 变量和 Tailwind CSS 工具类，导致样式管理混乱。经过重构，已完全移除 Tailwind CSS 依赖，采用原生 CSS 模块化架构。

#### 转换内容
1. **Login.jsx** - 将复杂的 Tailwind 类名转换为语义化 CSS 模块
   - 创建 `Login.module.css`
   - 定义 `.loginContainer`、`.loginCard`、`.loginForm` 等类名
   - 将内联 keyframe 动画移至 CSS 文件
   - 使用 `:global()` 处理 Ant Design 组件样式覆盖

2. **MainLayout.jsx** - 重构主布局样式
   - 创建 `MainLayout.module.css`
   - 定义 `.mainLayout`、`.sider`、`.header`、`.content` 等类名
   - 保留响应式媒体查询
   - 实现毛玻璃效果（backdrop-filter）

#### CSS 模块化规范

**文件命名**：
- 格式：`[ComponentName].module.css`
- 位置：与组件文件同目录
- 引入：`import styles from './ComponentName.module.css'`

**类名规范**：
- 使用 camelCase 命名法
- 体现组件层级和功能
- 示例：`.loginContainer`、`.headerToggle`、`.userDropdown`

**样式组织**：
- 所有颜色、间距、字体使用 CSS 变量
- Ant Design 组件覆盖使用 `:global()` 选择器
- 响应式样式使用媒体查询
- 动画定义在文件顶部

**变量使用强制规范**：
- ✅ 颜色：`var(--color-*)`
- ✅ 间距：`var(--spacing-*)`
- ✅ 字体大小：`var(--font-size-*)`
- ✅ 圆角：`var(--border-radius-*)`
- ✅ 阴影：`var(--shadow-*)`
- ✅ 过渡时长：`var(--transition-*)`

#### 技术收益
- ✅ 统一样式架构，全面使用原生 CSS
- ✅ 提升代码可维护性和可读性
- ✅ 减少构建依赖，无需 Tailwind 工具链
- ✅ 更好地支持 Notion 风格设计规范
- ✅ CSS 模块化天然支持作用域隔离

## 🚀 最佳实践

### 样式编写
1. **保持一致性** - 使用 CSS 变量而非硬编码值
2. **语义化命名** - 使用描述性的变量名和类名
3. **层次分明** - 通过颜色和字重区分信息层级
4. **性能优先** - 避免过度使用阴影和动画
5. **可访问性** - 确保足够的颜色对比度

### CSS 模块化
1. **组件独立** - 每个组件对应独立的 CSS 模块文件
2. **作用域隔离** - 利用 CSS 模块的自动作用域隔离
3. **全局样式** - Ant Design 组件覆盖使用 `:global()` 选择器
4. **充分注释** - 为复杂样式添加说明注释
5. **变量优先** - 所有设计 token 必须使用 CSS 变量

## 📝 注意事项

- 所有新增样式都遵循 Notion/Youmind 的设计语言
- 保持了原有的响应式布局
- 兼容现有的 Ant Design 组件
- 可通过修改 CSS 变量快速调整整体风格

---

**设计灵感来源**：Notion、Youmind、Linear
**更新日期**：2025-12-01
**版本**：1.0.0
